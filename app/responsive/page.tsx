import { ResponsiveLab } from "@/components/responsive/ResponsiveLab"

// Index for how a panel reaches a phone: the current presentation, with a launch link
// into the real app so the UX can be tested on a device.
export default function ResponsivePage() {
  return <ResponsiveLab />
}
